# Códigos antigos

Essa é uma "refatoração" dos códigos usados para calcular os fatores e escores originalmente. Os códigos em si não foram alterados, apenas os colocamos num único arquivo (`final.py`) para que as funções possam ser executadas como um script. Ademais, selecionamos um jupyter notebook que mostra como as funções devem ser usadas.
