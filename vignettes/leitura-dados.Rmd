---
title: "leitura-dados"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{leitura-dados}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(snis)
```

Teste

```{r}
for (ano in 2010:2022) {
  df = read(ano)
  na_cols = names(df)[sapply(df, function(x) all(is.na(x)))]
  if (length(na_cols))  cat(ano, "\t", na_cols, "\n")
}
```
